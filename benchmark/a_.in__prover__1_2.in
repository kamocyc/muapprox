%HES
Sentry =v
  ∀x. ∀y. /* error in iwayama solver (which seem not to be able to solve this formula) */
    y < 1 + 1 * x \/ y < 1 + (-1) * x \/ y < 1 \/ Exists x (\k. k x) y.

Exists _x fx y =v
  (X _x fx y \/ X _x fx (-y) \/ Exists _x fx (y - 1))
  /\ y >= 0.

X _x fx y =v X _x fx y /\ fx (\x. x = y).
