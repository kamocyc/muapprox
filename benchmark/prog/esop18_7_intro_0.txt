/* this is invalid since "b" may occur only finitely */
%HES
let f = if * then (event a; f) else (event b; f)
let () = f

%INITIAL
qa

%TRANSITION
(qa, a) -> qa
(qb, a) -> qa
(qa, b) -> qb
(qb, b) -> qb

%PRIORITY
qa -> 0
qb -> 1
