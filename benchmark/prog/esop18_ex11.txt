%PROGRAM
let g k = if * then (event a; k) else (event b; k)
let f x = if x > 0 then g (f (x - 1)) else (event b; f 5)
let () = f 5

/*
%ENV
g : ((qa, 0) /\ (qb, 1) -> qa, 0)
g : ((qa, 0) /\ (qb, 1) -> qb, 0)
f : (int -> qa, 0)
f : (int -> qb, 1)
*/

%INITIAL
qa

%TRANSITION
(qa, a) -> qa
(qb, a) -> qa
(qa, b) -> qb
(qb, b) -> qb

%PRIORITY
qa -> 0
qb -> 1
