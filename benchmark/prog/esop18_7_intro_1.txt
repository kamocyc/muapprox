/* this is invalid since "b" may NOT occur only finitely, and the result is negated in conversion */
%PROGRAM
let g k = if * then (event a; k) else (event b; k)
let f = g f
let () = f

%TRANSITION
qa a -> qa
qa b -> qb

qb a -> qa
qb b -> qb

%PRIORITY
qa -> 0
qb -> 1
