(* L(A) /\ Trace(P) = \emptyset ではない <=> 式がinvalid となる *)
%HES
let main = event a; main
let () = main

%ENV
main : (qa, 0)

%INITIAL
qa

%TRANSITION
(qa, a) -> qa
(qa, b) -> qb
(qb, a) -> qa
(qb, b) -> qb

%PRIORITY
qa -> 0
qb -> 1

(*
1. プログラムのsimple typeに対するcanonical intersection type をつくる
  * parity automaton の state, max priority から作られる
2. Fig. 6. の規則に従って、プログラムを変換する
3. 
*)