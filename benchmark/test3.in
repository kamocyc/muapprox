%HES
S =v F1 10 (F2 (x + 1)).
F1 x k =u F3 k.
F2 x y =u x > 20 /\ y > 30 /\ F1 (x + y) (F2 (x + 1)).
F3 f =u f 20 /\ F1 30 (F2 1).

/*
/* 複数の再帰変数の同時導入 */
%HES
S =v F1 10 /\ F3 35.
F1 x =u F3 (x + 1).
F3 x =u x > 20 /\ F1 (x + 25) /\ F3 (x + 30).*/

/*
/* 複数の再帰変数がある場合に正しい */
%HES
S =v F1 10.
F1 x =u F3 (x + 1).
F3 x =u x > 20 /\ F1 (x + 25) /\ F3 (x + 30).
*/

/*
/* F2の左辺の引数の生成がただしい */ 
%HES
S =v F1 10.
F1 x =u F2 5 (\y. y = 1) x.
F2 x1 x2 x3 =v F1 (x1 + 5) /\ (x2 1) /\ (x3 = 15).
*/

/* F2 x (\y. y = 1) / x3 */
/*
1. 述語への変数引数の追加で、to_abs' rec引数 型から引数
2. 本体への適用で、型から引数
*/

/*
F1 x = F2 (F3 F2) x.
F2 f x =u f x.
F3 f x =v x > 10 /\ f (x + 5).

let f3 f x = x > 10 && f (x + 5)
let f2 f x = f x
let f1 = f2 (f3 f2)

*/