/*
必要な再帰回数: n + 2 * m  なので、lexicogprahicでなくてもいい (iter=4で解ける) が、係数が大きすぎてタイムアウトする模様（lexicogprahicだと解ける）
*/
%HES
Sentry =u ∀n. ∀m. F n m (\r. true).
F m n k =u
  (m > 0 => F (m-1) n k) /\
  ((m <= 0 /\ n > 0) => F (m+1) (n-1) k) /\
  ((m <= 0 /\ n <= 0) => k false).
/* 条件の式の問題で、プログラムにしたときに多くの再帰が発生していた。（必要な再帰の深さの最大は変わらない） */
