/* 誤ったencoding */
/* inliningなしでのkatsura-solverでの正しい解はunknownのはず */
%HES
S =v
  ∀n. ∀e.
    e < 1 + 1 * n \/ e < 1 + (-1) * n \/ Main_e e n.

Main_e e n =v (App e (Eq n) n \/ Main_e (e - 1) n) /\ e >= 0.

App e f x =v f x.

Eq m n =v m = n.
